export interface StoreDetail {
  id: number;
  name: string;
  address: string;
  description: string;
  naverUrl?: string;
}
export interface StoreFormData {
  name: string;
  address: string;
  description: string;
  naverUrl?: string;
}
